VITE_API_BASE_URL=http://localhost:8000/api/v1

# Production (Vercel): set this BEFORE build to your Render backend, e.g.
# VITE_API_BASE_URL=https://medical-cross-checker-api.onrender.com/api/v1
# Vite bakes this at build time — changing it later requires a rebuild.
